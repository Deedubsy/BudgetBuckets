# Firebase App Hosting configuration for Budget Buckets

# Build settings
env:
  - variable: NODE_ENV
    value: production

# Runtime settings
runConfig:
  runtime: nodejs20
  minInstances: 0
  maxInstances: 10
  concurrency: 100
  cpu: 1
  memoryMiB: 512

# Environment variables for the app
secrets: []